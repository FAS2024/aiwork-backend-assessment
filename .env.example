# No .env is needed at the repository root.
# Copy env from each service's .env.example into that service's .env:
#   cp python-service/.env.example python-service/.env
#   cp ts-service/.env.example     ts-service/.env
